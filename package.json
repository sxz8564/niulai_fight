{
  "name": "niulai-fight",
  "version": "0.1.0",
  "private": true,
  "description": "A belt-scrolling brawler: Niulai against the wolves, left to right.",
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.js --bundle --format=esm --outfile=dist/bundle.js --target=es2022",
    "watch": "npm run build -- --watch",
    "serve": "node tools/serve.mjs",
    "test": "node tools/smoke-test.mjs",
    "package": "node tools/package.mjs",
    "test:extension": "node tools/extension-test.mjs"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "playwright": "^1.56.0"
  }
}
